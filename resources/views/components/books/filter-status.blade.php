<a href="{{ $url }}" class="flex justify-center items-center gap-2 py-2 px-3 rounded-lg
    {{ $selected ? 'text-white bg-blue-800' : 'text-blue-800 border border-blue-800'}}">
    <span>{{ $label }}</span>
</a>
